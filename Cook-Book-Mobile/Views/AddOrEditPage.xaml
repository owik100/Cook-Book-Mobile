<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Cook_Book_Mobile.Views.AddOrEditPage"  
             BindingContext="{Binding AddOrEdit, Source={StaticResource Locator}}" 
             Title="{Binding Title}">
    
    
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Nazwa:" HorizontalOptions="Center" FontSize="Medium"/>
            <Entry Placeholder="Nazwa przepisu" Text="{Binding RecipeName}"/>

            <Label Text="Przepis:" HorizontalOptions="Center" FontSize="Medium"/>
            <Entry Placeholder="Podaj przepis" Text="{Binding RecipeInstructions}"/>

            <Button Text="Dodaj" Command="{Binding SubmitCommand}" IsEnabled="{Binding CanRecipeSubmit}" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>